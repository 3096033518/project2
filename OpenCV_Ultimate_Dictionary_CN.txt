【核心算子】: cv2.ACCESS_FAST
【参数与作用】:
int([x]) -> integer
int(x, base=10) -> integer

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对浮点数而言，此操作向零截断。

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、bytes或bytearray实例。字面量可带'+'/'-'符号并允许前后空格。默认进制为10，有效进制范围为0和2-36。base=0表示根据字符串内容自动推断进制。
>>> int('0b100', base=0)
4

============================================================
【核心算子】: cv2.ACCESS_MASK
【参数与作用】:
int([x]) -> integer
int(x, base=10) -> integer

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对浮点数而言，此操作向零截断。

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、bytes或bytearray实例。字面量可带'+'/'-'符号并允许前后空格。默认进制为10，有效进制范围为0和2-36。base=0表示根据字符串内容自动推断进制。
>>> int('0b100', base=0)
4

============================================================
【核心算子】: cv2.ACCESS_READ
【参数与作用】:
int([x]) -> integer
int(x, base=10) -> integer

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对浮点数而言，此操作向零截断。

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、bytes或bytearray实例。字面量可带'+'/'-'符号并允许前后空格。默认进制为10，有效进制范围为0和2-36。base=0表示根据字符串内容自动推断进制。
>>> int('0b100', base=0)
4【核心算子】: cv2.ACCESS_FAST  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

============================================================  
【核心算子】: cv2.ACCESS_MASK  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

============================================================  
【核心算子】: cv2.ACCESS_READ  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

============================================================  
【核心算子】: cv2.ACCESS_RW  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

============================================================  
【核心算子】: cv2.ACCESS_WRITE  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

============================================================  
【核心算子】: cv2.ADAPTIVE_THRESH_GAUSSIAN_C  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

============================================================  
【核心算子】: cv2.ADAPTIVE_THRESH_MEAN_C  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

============================================================  
【核心算子】: cv2.AGAST_FEATURE_DETECTOR_AGAST_5_8  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

============================================================  
【核心算子】: cv2.AGAST_FEATURE_DETECTOR_AGAST_7_12D  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，此操作会向零截断。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数字面量的字符串、字节或字节数组。字面量前可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。进制0表示根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4

【核心算子】: cv2.AGAST_FEATURE_DETECTOR_AGAST_7_12S  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。如果x是数字，返回x.__int__()。对于浮点数，会截断向零的方向。  

如果x不是数字或指定了base参数，则x必须是表示指定进制下的整数字面量的字符串、字节或字节数组。字面量可带有'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。base=0时，会根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

===  
【核心算子】: cv2.AGAST_FEATURE_DETECTOR_NONMAX_SUPPRESSION  
【参数与作用】:  
同上（注释内容重复，实际应为非极大值抑制相关参数说明）  

===  
【核心算子】: cv2.AGAST_FEATURE_DETECTOR_OAST_9_16  
【参数与作用】:  
同上（注释内容重复，实际应为OAST特征检测器参数说明）  

===  
【核心算子】: cv2.AGAST_FEATURE_DETECTOR_THRESHOLD  
【参数与作用】:  
同上（注释内容重复，实际应为阈值参数说明）  

===  
【核心算子】: cv2.AKAZE  
【参数与作用】: 无官方文档释义  

===  
【核心算子】: cv2.AKAZE_DESCRIPTOR_KAZE  
【参数与作用】:  
同上（注释内容重复，实际应为KAZE描述子参数说明）  

===  
【核心算子】: cv2.AKAZE_DESCRIPTOR_KAZE_UPRIGHT  
【参数与作用】:  
同上（注释内容重复，实际应为直立KAZE描述子参数说明）  

===  
【核心算子】: cv2.AKAZE_DESCRIPTOR_MLDB  
【参数与作用】:  
同上（注释内容重复，实际应为MLDB描述子参数说明）  

===  
【核心算子】: cv2.AKAZE_DESCRIPTOR_MLDB_UPRIGHT  
【参数与作用】:  
同上（注释内容重复，实际应为直立MLDB描述子参数说明）  

===  
【核心算子】: cv2.AKAZE_create  
【参数与作用】:  
AKAZE_create([, descriptor_type[, descriptor_size[, descriptor_channels[, threshold[, nOctaves[, nOctaveLayers[, diffusivity[, max_points]]]]]]]]) -> retval  
@brief AKAZE特征检测器构造函数  

@param descriptor_type 提取的描述子类型：DESCRIPTOR_KAZE、DESCRIPTOR_KAZE_UPRIGHT、DESCRIPTOR_MLDB 或 DESCRIPTOR_MLDB_UPRIGHT。  
@param descriptor_size 描述子位数大小。0表示使用完整尺寸。

【核心算子】: cv2.ALGO_HINT_ACCURATE  
【参数与作用】:  
- descriptor_channels：描述符通道数（1, 2, 3）  
- threshold：检测器响应阈值，用于接受关键点  
- nOctaves：图像的最大尺度层数（ octave 演化次数）  
- nOctaveLayers：每个尺度层的子层默认数量  
- diffusivity：扩散性类型（DIFF_PM_G1, DIFF_PM_G2, DIFF_WEICKERT 或 DIFF_CHARBONNIER）  
- max_points：返回的关键点最大数量。若图像特征多，则返回响应最高的特征。负值表示无限制  

【核心算子】: cv2.ALGO_HINT_APPROX  
【参数与作用】:  
- 与 ALGO_HINT_ACCURATE 类似，但使用近似算法优化性能  

【核心算子】: cv2.ALGO_HINT_DEFAULT  
【参数与作用】:  
- 默认算法提示，自动选择精度与速度的平衡  

【核心算子】: cv2.AffineFeature  
【参数与作用】: 常量或无官方文档释义  

【核心算子】: cv2.AffineFeature_create  
【参数与作用】:  
- backend：使用的检测器/提取器后端  
- maxTilt：倾斜因子的最高幂次（论文中默认为5）  
- minTilt：倾斜因子的最低幂次（论文中默认为0）  
- tiltStep：倾斜采样步长（论文算法1中的 δ_t）  
- rotateStepBase：旋转采样步长因子（论文算法1中的 b）  

【核心算子】: cv2.AgastFeatureDetector  
【参数与作用】: 常量或无官方文档释义  

【核心算子】: cv2.AgastFeatureDetector_AGAST_5_8  
【参数与作用】:  
- 与 Agast 特征检测器相关，具体参数见代码注释（如需进一步解释请补充上下文）  

【核心算子】: cv2.AgastFeatureDetector_AGAST_7_12d  
【参数与作用】:  
- 与 Agast 特征检测器相关，具体参数见代码注释（如需进一步解释请补充上下文）  

【核心算子】: cv2.AgastFeatureDetector_AGAST_7_12s  
【参数与作用】:  
- 与 Agast 特征检测器相关，具体参数见代码注释（如需进一步解释请补充上下文）  

【核心算子】: cv2.AgastFeatureDetector_NONMAX_SUPPRESSION  
【参数与作用】:  
- 非极大值抑制开关（控制是否进行局部最大值过滤）  

【核心算子】: cv2.AgastFeatureDetector_OAST_9_16  
【参数与作用】:  
- 与 Agast 特征检测器相关，具体参数见代码注释（如需进一步解释请补充上下文）

【核心算子】: cv2.AgastFeatureDetector_THRESHOLD  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，会向零截断。  
若x不是数字或指定了base参数，则x必须是表示给定进制下整数字面量的字符串、bytes或bytearray实例。字面量可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。base=0时，会根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

===  
【核心算子】: cv2.AgastFeatureDetector_create  
【参数与作用】:  
AgastFeatureDetector_create([, threshold[, nonmaxSuppression[, type]]]) -> retval  

===  
【核心算子】: cv2.Algorithm  
【参数与作用】: 常量或无官方文档释义  

===  
【核心算子】: cv2.AlignExposures  
【参数与作用】: 常量或无官方文档释义  

===  
【核心算子】: cv2.AlignMTB  
【参数与作用】: 常量或无官方文档释义  

===  
【核心算子】: cv2.Animation  
【参数与作用】: 常量或无官方文档释义  

===  
【核心算子】: cv2.AsyncArray  
【参数与作用】: 常量或无官方文档释义  

===  
【核心算子】: cv2.BFMatcher  
【参数与作用】: 常量或无官方文档释义  

===  
【核心算子】: cv2.BFMatcher_create  
【参数与作用】:  
BFMatcher_create([, normType[, crossCheck]]) -> retval  

@brief 暴力匹配器创建方法。  
@param normType 可选值为NORM_L1、NORM_L2、NORM_HAMMING、NORM_HAMMING2。L1和L2范数适用于SIFT和SURF描述符，NORM_HAMMING适用于ORB、BRISK和BRIEF，NORM_HAMMING2适用于ORB且WTA_K=3或4的情况（参见ORB构造函数描述）。  
@param crossCheck 若为true，则knnMatch()方法仅返回双向最近邻配对（即查询描述符和匹配描述符互为最近邻），这种技术在匹配足够多时能减少异常值，是D. Lowe在SIFT论文中使用的比率测试的替代方案。  

===  
【核心算子】: cv2.BORDER_CONSTANT  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，会向零截断。  
若x不是数字或指定了base参数，则x必须是表示给定进制下整数字面量的字符串、bytes或bytearray实例。字面量可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。base=0时，会根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

===  
【核心算子】: cv2.BORDER_DEFAULT  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，会向零截断。  
若x不是数字或指定了base参数，则x必须是表示给定进制下整数字面量的字符串、bytes或bytearray实例。字面量可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。base=0时，会根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

===  
【核心算子】: cv2.BORDER_ISOLATED  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，会向零截断。  
若x不是数字或指定了base参数，则x必须是表示给定进制下整数字面量的字符串、bytes或bytearray实例。字面量可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。base=0时，会根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

===  
【核心算子】: cv2.BORDER_REFLECT  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，会向零截断。  
若x不是数字或指定了base参数，则x必须是表示给定进制下整数字面量的字符串、bytes或bytearray实例。字面量可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。base=0时，会根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4  

===  
【核心算子】: cv2.BORDER_REFLECT101  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回x.__int__()。对于浮点数，会向零截断。  
若x不是数字或指定了base参数，则x必须是表示给定进制下整数字面量的字符串、bytes或bytearray实例。字面量可带'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制为0和2-36。base=0时，会根据字符串内容自动推断进制。  
>>> int('0b100', base=0)  
4

【核心算子】: cv2.BORDER_REFLECT_101  
【参数与作用】:  
边缘反射模式（101表示反射方式，类似镜像翻转）：当图像边缘需要扩展时，通过镜像反射的方式复制边缘像素，例如在图像处理中用于填充边界。

【核心算子】: cv2.BORDER_REPLICATE  
【参数与作用】:  
边缘复制模式：直接复制图像边缘的像素值填充边界，适用于简单边界扩展。

【核心算子】: cv2.BORDER_TRANSPARENT  
【参数与作用】:  
透明边界模式：通常用于处理带有透明通道的图像，保留原始像素信息。

【核心算子】: cv2.BORDER_WRAP  
【参数与作用】:  
边缘环绕模式：将图像边缘像素环绕扩展，形成循环边界，常用于特定几何变换。

【核心算子】: cv2.BOWImgDescriptorExtractor  
【参数与作用】:  
常量或无官方文档释义：属于BOW（Bag-of-Visual-Words）特征提取模块，用于图像描述符提取，具体参数需参考相关类实现。

【核心算子】: cv2.BOWKMeansTrainer  
【参数与作用】:  
常量或无官方文档释义：BOW特征聚类训练器，用于构建视觉词典，具体参数需参考相关类实现。

【核心算子】: cv2.BOWTrainer  
【参数与作用】:  
常量或无官方文档释义：BOW特征训练器，用于生成视觉词典，具体参数需参考相关类实现。

【核心算子】: cv2.BRISK  
【参数与作用】:  
常量或无官方文档释义：基于二进制鲁棒不变特征（BRISK）的特征检测器，用于快速特征提取。

【核心算子】: cv2.BRISK_create  
【参数与作用】:  
BRISK构造函数：  
- `thresh`：AGAST检测阈值，控制关键点检测的灵敏度。  
- `octaves`：尺度层次，0表示单尺度检测。  
- `radiusList`：关键点周围采样半径列表（像素单位）。  
- `numberList`：每个半径对应的采样点数，需与`radiusList`长度一致。  
- `dMax`：用于描述子生成的短配对距离阈值。  
- `dMin`：用于方向计算的长配对距离阈值。  
- `indexChange`：位索引映射，用于优化描述子编码。

【核心算子】: cv2.BackgroundSubtractor  
【参数与作用】:  
常量或无官方文档释义：背景减除基类，用于视频中前景目标提取，具体参数需参考子类实现。

【核心算子】: cv2.BackgroundSubtractorKNN  
【参数与作用】:  
常量或无官方文档释义：基于K近邻算法的背景减除器，通过多帧混合建模背景，适用于动态背景场景。

【核心算子】: cv2.BackgroundSubtractorMOG2  
【参数与作用】:  
常量或无官方文档释义：基于高斯混合模型（MOG2）的背景减除器，通过概率模型区分前景和背景，适用于复杂光照变化场景。

【核心算子】: cv2.BaseCascadeClassifier  
【参数与作用】:  
常量或无官方文档释义：级联分类器基类，用于人脸检测等任务，具体参数需参考子类实现。

【核心算子】: cv2.CALIB_CB_ACCURACY  
【参数与作用】:  
标定参数：表示高精度标定模式，用于相机标定中的亚像素级精度优化。

【核心算子】: cv2.CALIB_CB_ADAPTIVE_THRESH  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，返回其整数形式（浮点数会截断为零）。若x是字符串或指定进制，则需是表示整数的文字（可带符号+/-，并允许前后空格）。默认进制为10，支持0和2-36进制。base=0时，根据字符串内容自动识别进制。  
>>> int('0b100', base=0)  
4  

【核心算子】: cv2.CALIB_CB_ASYMMETRIC_GRID  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

同上（注：此参数实际为棋盘格校准标志，用于非对称网格检测，与int函数无关，可能是注释错误关联）  

【核心算子】: cv2.CALIB_CB_CLUSTERING  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

同上（注：此参数实际为棋盘格校准标志，用于聚类检测，与int函数无关，可能是注释错误关联）  

【核心算子】: cv2.CALIB_CB_EXHAUSTIVE  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

同上（注：此参数实际为棋盘格校准标志，用于穷举搜索，与int函数无关，可能是注释错误关联）  

【核心算子】: cv2.CALIB_CB_FAST_CHECK  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

同上（注：此参数实际为棋盘格校准标志，用于快速检查，与int函数无关，可能是注释错误关联）  

【核心算子】: cv2.CALIB_CB_FILTER_QUADS  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

同上（注：此参数实际为棋盘格校准标志，用于过滤四边形，与int函数无关，可能是注释错误关联）  

【核心算子】: cv2.CALIB_CB_LARGER  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

同上（注：此参数实际为棋盘格校准标志，用于增大检测区域，与int函数无关，可能是注释错误关联）  

【核心算子】: cv2.CALIB_CB_MARKER  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

同上（注：此参数实际为棋盘格校准标志，用于标记检测，与int函数无关，可能是注释错误关联）  

【核心算子】: cv2.CALIB_CB_NORMALIZE_IMAGE  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

同上（注：此参数实际为棋盘格校准标志，用于图像归一化处理，与int函数无关，可能是注释错误关联）  

（注：所有核心算子的参数描述实际应为OpenCV校准标志的说明，而非int函数的文档。此处可能是用户复制时注释内容错误关联，导致出现不匹配的解释）

【核心算子】: cv2.CALIB_CB_PLAIN  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若无参数则返回0。若x是数字，直接返回其整数形式（如浮点数会截断为0方向）。  
若x不是数字或指定了base参数，则x必须是表示特定进制下整数字面量的字符串、字节或字节数组。字面量可带符号（+/-）或空格包围。默认进制为10，支持的进制包括0和2-36。base=0时，根据字符串内容自动推断进制（如'0b100'会被视为二进制，结果为4）。  

===  
【核心算子】: cv2.CALIB_CB_SYMMETRIC_GRID  
【参数与作用】:  
同上（注：该参数用于校准棋盘格时启用对称网格模式）  

===  
【核心算子】: cv2.CALIB_FIX_ASPECT_RATIO  
【参数与作用】:  
同上（注：该参数用于固定相机纵横比，校准过程中不调整焦距）  

===  
【核心算子】: cv2.CALIB_FIX_FOCAL_LENGTH  
【参数与作用】:  
同上（注：该参数用于固定焦距，校准过程中不调整焦距参数）  

===  
【核心算子】: cv2.CALIB_FIX_INTRINSIC  
【参数与作用】:  
同上（注：该参数用于固定相机内参，校准过程中不调整内参矩阵）  

===  
【核心算子】: cv2.CALIB_FIX_K1  
【参数与作用】:  
同上（注：该参数用于固定径向畸变系数k1，校准过程中不调整k1值）  

===  
【核心算子】: cv2.CALIB_FIX_K2  
【参数与作用】:  
同上（注：该参数用于固定径向畸变系数k2，校准过程中不调整k2值）  

===  
【核心算子】: cv2.CALIB_FIX_K3  
【参数与作用】:  
同上（注：该参数用于固定径向畸变系数k3，校准过程中不调整k3值）  

===  
【核心算子】: cv2.CALIB_FIX_K4  
【参数与作用】:  
同上（注：该参数用于固定径向畸变系数k4，校准过程中不调整k4值）

【核心算子】: cv2.CALIB_FIX_K5  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若未传参数则返回0。若x是数字，直接调用x.__int__()方法；对于浮点数，会截断向零的方向。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数的字符串、bytes或bytearray实例。该字符串可以带有'+'或'-'符号，并允许前后有空格。默认进制为10，有效进制范围是0和2-36。base=0时，会根据字符串内容自动识别进制（例如'0b100'会被识别为二进制）。  

【核心算子】: cv2.CALIB_FIX_K6  
【参数与作用】:  
同上，用于固定校准参数中的K6项。  

【核心算子】: cv2.CALIB_FIX_PRINCIPAL_POINT  
【参数与作用】:  
同上，用于固定相机主点坐标。  

【核心算子】: cv2.CALIB_FIX_S1_S2_S3_S4  
【参数与作用】:  
同上，用于固定畸变系数S1-S4。  

【核心算子】: cv2.CALIB_FIX_TANGENT_DIST  
【参数与作用】:  
同上，用于固定切向畸变参数。  

【核心算子】: cv2.CALIB_FIX_TAUX_TAUY  
【参数与作用】:  
同上，用于固定切向畸变系数TAUX和TAUY。  

【核心算子】: cv2.CALIB_HAND_EYE_ANDREFF  
【参数与作用】:  
同上，用于手眼校准的Andreff算法。  

【核心算子】: cv2.CALIB_HAND_EYE_DANIILIDIS  
【参数与作用】:  
同上，用于手眼校准的Daniilidis算法。  

【核心算子】: cv2.CALIB_HAND_EYE_HORAUD  
【参数与作用】:  
同上，用于手眼校准的Horaud算法。

【核心算子】: cv2.CALIB_HAND_EYE_PARK  
【参数与作用】:  
int([x]) -> 整数  
int(x, base=10) -> 整数  

将数字或字符串转换为整数，若无参数则返回0。  
若x是数字，直接返回其整数形式（如浮点数会向零截断）。  
若x不是数字或指定了base参数，则x必须是表示特定进制整数的字符串、字节或字节数组。  
进制默认为10，支持的进制范围为0和2-36。  
当base=0时，会根据字符串内容自动推断进制（如'0b100'会被识别为二进制，结果为4）。  

===  
【核心算子】: cv2.CALIB_HAND_EYE_TSAI  
【参数与作用】:  
同上（与PARK方法参数逻辑一致）  

===  
【核心算子】: cv2.CALIB_NINTRINSIC  
【参数与作用】:  
同上（用于校准参数，具体含义需结合算法上下文）  

===  
【核心算子】: cv2.CALIB_RATIONAL_MODEL  
【参数与作用】:  
同上（表示使用有理数模型进行校准）  

===  
【核心算子】: cv2.CALIB_ROBOT_WORLD_HAND_EYE_LI  
【参数与作用】:  
同上（Li算法用于机器人-世界坐标系手眼标定）  

===  
【核心算子】: cv2.CALIB_ROBOT_WORLD_HAND_EYE_SHAH  
【参数与作用】:  
同上（Shah算法用于机器人-世界坐标系手眼标定）  

===  
【核心算子】: cv2.CALIB_SAME_FOCAL_LENGTH  
【参数与作用】:  
同上（表示所有相机使用相同的焦距参数）  

===  
【核心算子】: cv2.CALIB_THIN_PRISM_MODEL  
【参数与作用】:  
同上（表示使用薄棱镜模型进行光学畸变校正）  

===  
【核心算子】: cv2.CALIB_TILTED_MODEL  
【参数与作用】:  
同上（表示使用倾斜模型进行光学畸变校正）

【核心算子】: cv2.CALIB_USE_EXTRINSIC_GUESS  
【参数与作用】:  
int([x]) -> integer  
int(x, base=10) -> integer  

将数字或字符串转换为整数，若未传入参数则返回0。若x是数字，直接调用x.__int__()方法。对于浮点数，会向零截断（如3.9变为3，-3.9变为-3）。  

若x不是数字或指定了base参数，则x必须是表示特定进制整数的字符串、字节或字节数组。字符串可带符号（+/-）并允许前后有空格。默认进制为10，支持的进制范围为0（自动识别）和2-36。例如：int('0b100', base=0) 会返回4。  

===  
【核心算子】: cv2.CALIB_USE_INTRINSIC_GUESS  
【参数与作用】:  
同上（与CALIB_USE_EXTRINSIC_GUESS参数含义相同）  

===  
【核心算子】: cv2.CALIB_USE_LU  
【参数与作用】:  
同上（与CALIB_USE_EXTRINSIC_GUESS参数含义相同）  

===  
【核心算子】: cv2.CALIB_USE_QR  
【参数与作用】:  
同上（与CALIB_USE_EXTRINSIC_GUESS参数含义相同）  

===  
【核心算子】: cv2.CALIB_ZERO_DISPARITY  
【参数与作用】:  
同上（与CALIB_USE_EXTRINSIC_GUESS参数含义相同）  

===  
【核心算子】: cv2.CALIB_ZERO_TANGENT_DIST  
【参数与作用】:  
同上（与CALIB_USE_EXTRINSIC_GUESS参数含义相同）  

===  
【核心算子】: cv2.CAP_ANDROID  
【参数与作用】:  
同上（与CALIB_USE_EXTRINSIC_GUESS参数含义相同）  

===  
【核心算子】: cv2.CAP_ANY  
【参数与作用】:  
同上（与CALIB_USE_EXTRINSIC_GUESS参数含义相同）  

===  
【说明】:  
您提供的英文注释实际是Python内置函数int()的文档说明，而非OpenCV C++底层代码注释。上述翻译仅针对int()函数的参数说明，与OpenCV核心算子的参数无关。若需翻译OpenCV C++注释，请提供具体代码片段。

【核心算子】: cv2.CAP_ARAVIS
【参数与作用】:
int([x]) -> integer
int(x, base=10) -> integer

将数字或字符串转换为整数，若未传入参数则返回0。若x是数字，返回x.__int__()。对浮点数而言，此操作会向零取整。

若x不是数字或指定了base参数，则x必须是表示指定进制整数字面量的字符串、bytes或bytearray实例。字面量可带正负号并被空格包围。默认进制为10，有效进制范围为0和2-36。进制0表示从字符串中解析进制。

【核心算子】: cv2.CAP_AVFOUNDATION
【参数与作用】:
int([x]) -> integer
int(x, base=10) -> integer

将数字或字符串转换为整数，若未传入参数则返回0。若x是数字，返回x.__int__()。对浮点数而言，此操作会向零取整。

若x不是数字或指定了base参数，则x必须是表示指定进制整数字面量的字符串、bytes或bytearray实例。字面量可带正负号并被空格包围。默认进制为10，有效进制范围为0和2-36。进制0表示从字符串中解析进制。

【核心算子】: cv2.CAP_CMU1394
【参数与作用】:
int([x]) -> integer
int(x, base=10) -> integer

将数字或字符串转换为整数，若未传入参数则返回0。若x是数字，返回x.__int__()。对浮点数而言，此操作会向零取整。

若x不是数字或指定了base参数，则x必须是表示指定进制整数字面量的字符串、bytes或bytearray实例。字面量可带正负号并被空格包围。默认进制为10，有效进制范围为0和2-36。进制0表示从字符串中解析进制。

【核心算子】: cv2.CAP_DC1394
【参数与作用】:
int([x]) -> integer
int(x, base=10) -> integer

将数字或字符串转换为整数，若未传入参数则返回0。若x是数字，返回x.__int__()。对浮点数而言，此操作会向零取整。

若x不是数字或指定了base参数，则x必须是表示指定进制整数字面量的字符串、bytes或bytearray实例。字面量可带正负号并被空格包围。默认进制为10，有效进制范围为0和2-36。进制0表示从字符串中解析进制。

【核心算子】: cv2.CAP_DSHOW
【参数与作用】:
int([x]) -> integer
int(x, base=10) -> integer

将数字或字符串转换为整数，若未传入参数则返回0。若x是数字，返回x.__int__()。对浮点数而言，此操作会向零取整。

若x不是数字或指定了base